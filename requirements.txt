streamlit>=1.35
plotly>=5.20
pandas>=2.0
scikit-learn>=1.4
numpy>=1.24
google-genai>=1.0

# Optional -- only required to use the Strategy 3 zero-shot classifier
# (zero_shot_model.py / Tab 3 "Zero-Shot Classifier" section). Needs
# network access to huggingface.co on first run to download the model.
# transformers>=4.40
# torch>=2.2
